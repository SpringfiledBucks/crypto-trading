此目录包含早期的 Python 实现的 webconsole。该 Python 实现已被弃用并迁移到 C++ 后端（`src/webconsole_server.cpp`）。

原始脚本已移入仓库归档以便查阅：`archive/`。请不要在生产环境中运行 `webconsole/server.py`。构建与运行请参见项目根目录 `README.md` 与 `docs/WEBCONSOLE.md`。
